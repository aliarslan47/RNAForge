# m10 KEGG Pathway ORA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DESeq2 DEG'leri için KEGG pathway ORA modülü (m10); m09 motorunu değiştirmeden yeniden kullanır.

**Architecture:** KEGG REST 3 dosyasından gen→pathway (sembol join, m09 deseni) → `enrichment.py` motoru (run_ora/bh_fdr/write_ora_tsv) DEĞİŞMEZ → enrichment.R parametrize (KEGG prefix) → m10 orkestratör (gate yok) → CLI `kegg` → m08 raporda KEGG alt-bölümü. Zincir m06→m07→m09/m10→m08.

**Tech Stack:** Python 3.11 stdlib, R/ggplot2 (rnaforge-de), pytest.

## Global Constraints

- Motor YENİDEN KULLANILIR: `deg_sets`/`run_ora`/`bh_fdr`/`write_ora_tsv`/`all_tested_genes` DEĞİŞMEZ.
- Sembol join belirsizse ATILIR (tahmin yok); `go_annotation._symbol_to_locus` yeniden kullanılır.
- Global/overview map filtresi: numerik id ∈ {01100,01110,01120,01200,01210,01212,01220,01230,01232,01240,01250} hariç.
- Namespace tek grup "KEGG" (tek BH grubu).
- Gate YOK; verdict m06/m07'den taşınır. Eksik KEGG dosyası/kod → gürültülü hata + talimat.
- DEG eşikleri: `config.de.fdr_threshold`/`log2fc_threshold`. min_term_size/top_n: `config.enrichment`.
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`
- m09 R yolu bozulmamalı (enrichment.R yeni args OPSIYONEL, varsayılan m09).

---

### Task 1: Config — KEGG alanları

**Files:**
- Modify: `rnaforge/config.py` (Enrichment dataclass + parse)
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Enrichment.kegg_organism: str|None`, `Enrichment.kegg_dir: Path|None`.

- [ ] **Step 1: Failing test:**
```python
def test_enrichment_kegg_fields(tmp_path):
    body = PROK_BODY + "\nenrichment:\n  kegg_organism: eco\n  kegg_dir: references/kegg/eco\n"
    cfg = load_config(_write(tmp_path, body))
    assert cfg.enrichment.kegg_organism == "eco"
    assert cfg.enrichment.kegg_dir.name == "eco"

def test_enrichment_kegg_defaults(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY))
    assert cfg.enrichment.kegg_organism is None and cfg.enrichment.kegg_dir is None
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `Enrichment`'e `kegg_organism: str|None=None`, `kegg_dir: Path|None=None`; parse'ta `kegg_organism=(str(enrichment_raw["kegg_organism"]) if enrichment_raw.get("kegg_organism") else None)`, `kegg_dir=(Path(...) if ... else None)`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m10): config KEGG organism/dir`.

---

### Task 2: KEGG parser → gene2pathway

**Files:**
- Create: `rnaforge/kegg_annotation.py`
- Test: `tests/test_kegg_annotation.py`

**Interfaces:**
- Consumes: `go_annotation._symbol_to_locus`, `go_annotation.parse_gff_go`.
- Produces: `parse_kegg(links_path, names_path, genelist_path) -> tuple[dict[str,set[str]], dict[str,tuple[str,str]]]`
  → `(symbol2pathways, pathway_meta{id:("KEGG",name)})`;
  `build_gene2pathway(gff_path, links, names, genelist) -> tuple[gene2pathway(locus_tag→set), pathway_meta, gene_symbol, stats]`.

- [ ] **Step 1: Failing test** (küçük 3 dosya fixture):
```python
LINKS = "eco:b0002\tpath:eco00260\neco:b0003\tpath:eco00260\neco:b0114\tpath:eco01100\n"  # 01100 global -> filtre
NAMES = "eco00260\tGlycine, serine and threonine metabolism - Escherichia coli\neco01100\tMetabolic pathways - E. coli\n"
GENELIST = "eco:b0002\tCDS\t337..2799\tthrA; fused ...\neco:b0003\tCDS\t..\tthrB; homoserine kinase\n"
def test_parse_kegg_symbol_pathways_and_filter(tmp_path):
    s2p, meta = parse_kegg(...)
    assert s2p["thrA"] == {"eco00260"}          # global map 01100 hariç
    assert meta["eco00260"] == ("KEGG", "Glycine, serine and threonine metabolism")  # org eki kırpıldı
    assert "eco01100" not in meta
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_kegg`: genelist'ten b-number→symbol (col4 ilk `;`/`,` öncesi); links'ten b-number→pathway (`path:` strip, global set hariç); names'ten pathway→ad (` - ` sonrası kırp). symbol2pathways birleştir. `build_gene2pathway`: `parse_gff_go(gff)` → gene_symbol; `_symbol_to_locus` (belirsiz atılır); symbol2pathways → locus_tag2pathways; stats(n_annotated, n_pathways).
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m10): KEGG REST parser + gene2pathway (symbol join)`.

---

### Task 3: Belirsiz sembol + kapsama testi

**Files:**
- Modify: `tests/test_kegg_annotation.py`

- [ ] **Step 1: Failing test:**
```python
def test_build_gene2pathway_drops_ambiguous_symbol(tmp_path):
    # GFF'te aynı sembol iki locus'a -> join'de atılır (yalancı yok)
    ...
    assert "LT_dupsym1" not in g2p and "LT_dupsym2" not in g2p
def test_build_gene2pathway_stats(tmp_path):
    assert stats["n_annotated"] >= 1
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** (Task 2'de zaten `_symbol_to_locus` belirsiz atıyor; test bunu sabitler — gerekiyorsa küçük düzeltme.)
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `test(m10): ambiguous-symbol drop + coverage`.

---

### Task 4: enrichment.R + runner parametrize (KEGG prefix)

**Files:**
- Modify: `rnaforge/scripts/enrichment.R`, `rnaforge/enrichment.py`
- Test: `tests/test_enrichment.py` (env-gated)

**Interfaces:**
- Produces: `run_enrichment_r(up_tsv, down_tsv, out_dir, top_n, title_prefix="GO zenginleştirme", basename_prefix="enrichment", env=...)`;
  `build_enrichment_manifest(fig_dir, basename_prefix="enrichment")`.

- [ ] **Step 1: Failing test:**
```python
def test_build_manifest_kegg_basename(tmp_path):
    (tmp_path/"kegg_up.png").write_bytes(b"x")
    m = build_enrichment_manifest(tmp_path, basename_prefix="kegg")
    assert [f["id"] for f in m["figures"]] == ["kegg_up"]
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `enrichment.R`: sonda opsiyonel `a[5]` title_prefix, `a[6]` basename_prefix oku (yoksa varsayılan); başlıkları prefix'ten kur ("<prefix> — Artan/Azalan"), dosya adı `<basename>_up/down`. `run_enrichment_r`: yeni opsiyonel args, cmd'ye ekle. `build_enrichment_manifest`: `basename_prefix` param, ENRICHMENT_FIGURE_SPECS'i prefikse göre kur (`f"{prefix}_up"` vb.). m09 çağrıları varsayılanla korunur.
- [ ] **Step 4:** PASS. m09 testleri hâlâ yeşil (regresyon yok).
- [ ] **Step 5:** commit `feat(m10): parametrize enrichment.R + manifest for reuse`.

---

### Task 5: m10 orkestratör + CLI

**Files:**
- Create: `rnaforge/modules/m10_kegg.py`
- Modify: `rnaforge/cli.py`
- Test: `tests/test_m10_kegg.py`

**Interfaces:**
- Consumes: `build_gene2pathway`, `deg_sets`, `all_tested_genes`, `run_ora`, `write_ora_tsv`, `run_enrichment_r`, `build_enrichment_manifest`, `RunState`.
- Produces: `run_kegg(config, metadata_path, run_dir, force=False) -> dict`; CLI `rnaforge kegg`.

- [ ] **Step 1: Failing test** (R monkeypatch):
```python
def test_run_kegg_requires_m06(tmp_path): ...        # ValueError
def test_run_kegg_requires_kegg_organism(tmp_path): ... # kegg_organism None -> ValueError
def test_run_kegg_missing_files_loud(tmp_path): ...   # kegg_dir dosyaları yok -> FileNotFoundError
def test_run_kegg_writes_outputs_and_stats(monkeypatch): ...  # kegg_up/down.tsv + kegg_statistics.json
def test_run_kegg_no_gate(tmp_path): ...              # gates.json'a dokunmaz
def test_run_kegg_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m10_kegg.py` m09 deseninden: dizin `kegg/`, RunState, resume, m06 ön koşul, kegg_organism/dosya yoksa gürültülü hata; `build_gene2pathway` → `deg_sets` → `run_ora`(up/down, min_term_size) → `write_ora_tsv(kegg/kegg_{up,down}.tsv)` → `run_enrichment_r(..., title_prefix="KEGG Pathway", basename_prefix="kegg")` → `build_enrichment_manifest(kegg_dir, "kegg")` → `kegg_statistics.json`; gate YOK. `_write_gene2pathway_tsv` denetim izi. `cli.py`: `kegg` subparser + `_cmd_kegg` + main dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m10): orchestrator + rnaforge kegg CLI`.

---

### Task 6: m08 raporda KEGG alt-bölümü

**Files:**
- Modify: `rnaforge/report_html.py`
- Test: `tests/test_report_html.py`

**Interfaces:**
- Consumes: `kegg_up.tsv`, `kegg_down.tsv`, kegg manifest.
- Produces: "Fonksiyonel Zenginleştirme" bölümünde KEGG alt-bölümü + Yöntemler/Kaynaklar KEGG (kegg_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_enrichment_kegg_subsection(tmp_path):
    inputs = {..., "kegg_up": _kegg_rows, "kegg_down": [], "kegg_manifest": None, "kegg_dir": tmp_path}
    html = section_enrichment(inputs, LABELS["tr"], "tr")
    assert "KEGG" in html and "<pathway term>" in html
def test_methods_and_refs_kegg_when_ran():
    withk = section_methods(cfg, LABELS["tr"], enrichment_ran=True, kegg_ran=True)
    assert "KEGG" in withk
    refs = section_references(LABELS["en"], enrichment_ran=True, kegg_ran=True)
    assert "10.1093/nar/28.1.27" in refs   # Kanehisa & Goto 2000
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `load_report_inputs`: `kegg_{up,down}` + `kegg_manifest` + `kegg_dir` (yoksa None). `section_enrichment`: GO alt-bölümünden sonra KEGG alt-bölümü (parse_enrichment_tsv aynı; tablo başlığı "KEGG"); manifest'ten kegg figürü göm. `LABELS`: kegg alt-başlık etiketleri; legend'e KEGG satırı. `section_methods(config, L, enrichment_ran, kegg_ran=False)` + `section_references(L, enrichment_ran, kegg_ran=False)`: KEGG cümlesi + Kanehisa&Goto kaynağı (`_ENRICHMENT_REFERENCES`'e KEGG ayrı ya da kegg_ran ile). `render_report`: `kegg_ran = inputs.get("kegg_up") is not None or inputs.get("kegg_down") is not None`; wire.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m10): m08 raporda KEGG alt-bölümü + yöntem/kaynak`.

---

### Task 7: KEGG prep + GSE300731 canlı smoke

**Files:**
- Create: `references/kegg/eco/{pathway_links.tsv,pathway_names.tsv,gene_list.tsv}` (gitignore'lu, REST)

- [ ] **Step 1:** KEGG REST indir: `curl -s rest.kegg.jp/link/pathway/eco > references/kegg/eco/pathway_links.tsv`; `.../list/pathway/eco > pathway_names.tsv`; `.../list/eco > gene_list.tsv`. Format doğrula (sütun/örnek). `.gitignore` (references/) kapsıyor — teyit.
- [ ] **Step 2:** GSE300731 config'ine `enrichment.kegg_organism: eco` + `kegg_dir` ekle (raw/, gitignore'lu).
- [ ] **Step 3:** `rnaforge kegg --run-id GSE300731` koştur; `kegg_up/down.tsv` + figür incele.
- [ ] **Step 4:** Biyolojik akıl kontrolü: DOWN oksidatif fosforilasyon/sitrat/respirasyon; UP two-component/AMP direnci/biyofilm gibi. Figür okunur mu (etiket sarma m09'dan geliyor).
- [ ] **Step 5:** `rnaforge report --force` → GO + KEGG birlikte; verdict SUSPECT değişmez. Doğrulama notu.

---

## Self-Review

**Spec coverage:** §2 annotation→Task 2-3; §3 motor→(değişmez, Task 5 kullanır); §4 figür→Task 4; §5 orkestrasyon/CLI→Task 5; §6 config→Task 1; §7 rapor→Task 6; §9 doğrulama→her task testleri + Task 7 smoke. Tümü kapsanıyor.

**Placeholder scan:** Kod adımları somut; yer tutucu yok (fixture'lar kısaltılmış ama imza+algoritma net).

**Type consistency:** `build_gene2pathway`→`run_kegg`; `run_enrichment_r`/`build_enrichment_manifest` yeni opsiyonel arg'ları Task 4↔5 tutarlı; `parse_enrichment_tsv` KEGG TSV'sini de okur (aynı sözleşme).
