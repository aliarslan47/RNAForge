# m12 Semantic Reduction (REVIGO) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GO ORA/GSEA terimlerini semantik benzerlikle (Lin, obo) temsilcilere indirgeyen m12 modülü.

**Architecture:** obo + m09 build_gene2go (IC) → Lin similarity → REVIGO-benzeri greedy indirgeme → reduced TSV → m08 REVIGO bölümü. Saf Python stdlib; figür yok; gate yok. Zincir m09/m11 sonrası.

**Tech Stack:** Python 3.11 stdlib, pytest.

## Global Constraints

- Saf Python stdlib (numpy YOK). obo/`_ancestors`/`build_gene2go` yeniden kullanılır.
- Benzerlik yalnız aynı namespace içinde. Gate YOK; verdict m06/m07'den taşınır.
- Eşik `config.enrichment.revigo_similarity` (0.7). Eksik obo/GO kaynağı → gürültülü hata.
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`

---

### Task 1: Config revigo_similarity

**Files:** Modify `rnaforge/config.py`; Test `tests/test_config.py`.
**Interfaces:** Produces `Enrichment.revigo_similarity: float = 0.7`.

- [ ] **Step 1: Failing test:**
```python
def test_enrichment_revigo_similarity(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY + "\nenrichment:\n  revigo_similarity: 0.5\n"))
    assert cfg.enrichment.revigo_similarity == 0.5
def test_enrichment_revigo_default(tmp_path):
    assert load_config(_write(tmp_path, PROK_BODY)).enrichment.revigo_similarity == 0.7
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `Enrichment`'e `revigo_similarity: float = 0.7`; parse `_as_float(enrichment_raw.get("revigo_similarity",0.7),...)`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m12): config revigo_similarity`.

---

### Task 2: semantic.py — IC + Lin

**Files:** Create `rnaforge/semantic.py`; Test `tests/test_semantic.py`.
**Interfaces:** Produces `compute_ic(gene2go)->dict[str,float]`; `lin_similarity(a,b,obo,ic,cache)->float`.

- [ ] **Step 1: Failing test:**
```python
def test_compute_ic_monotone():
    # kök (tüm genler) IC~0, nadir terim IC yüksek
    g2go={"g1":{"GO:1","GO:2"},"g2":{"GO:1"}}   # GO:1 tümünde, GO:2 birinde
    ic=compute_ic(g2go); assert ic["GO:2"]>ic["GO:1"]
def test_lin_identity_and_parent():
    obo={"GO:1":{"parents":set(),"namespace":"BP","obsolete":False},
         "GO:2":{"parents":{"GO:1"},"namespace":"BP","obsolete":False}}
    ic={"GO:1":0.5,"GO:2":2.0}
    assert lin_similarity("GO:2","GO:2",obo,ic,{})==1.0
    s=lin_similarity("GO:2","GO:1",obo,ic,{}); assert 0<=s<=1
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `compute_ic`: term_count via gene2go; N=len(gene2go); IC=-log(count/N); count 0 guard. `lin_similarity`: a==b→1; MICA = argmax IC over ({a}∪anc(a))∩({b}∪anc(b)) (`go_annotation._ancestors`); Lin=2*IC(MICA)/(IC(a)+IC(b)); payda 0 veya MICA yok→0.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m12): information content + Lin similarity`.

---

### Task 3: semantic.py — reduce_terms

**Files:** Modify `rnaforge/semantic.py`; Test `tests/test_semantic.py`.
**Interfaces:** Produces `reduce_terms(terms, obo, ic, threshold)->list[dict]` (terms: [{go_id,namespace,term,padj}]).

- [ ] **Step 1: Failing test:**
```python
def test_reduce_collapses_similar(monkeypatch):
    # GO:2 ve GO:3 (kardeş, benzer) -> tek temsilci; farklı namespace ayrı
    ... assert n_representatives < n_input; assert members doğru
def test_reduce_namespace_separate(): ...   # BP ve CC birleşmez
def test_reduce_empty(): assert reduce_terms([],...)==[]
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** namespace grupla; padj artan sırala; greedy: her terim reps'e max Lin≥threshold→o kümeye, değilse yeni temsilci. Dönüş: temsilci dict + `n_collapsed` + `members`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m12): REVIGO-like greedy reduction`.

---

### Task 4: m12 orkestratör + CLI

**Files:** Create `rnaforge/modules/m12_semantic.py`; Modify `rnaforge/cli.py`; Test `tests/test_m12_semantic.py`.
**Interfaces:** Produces `run_semantic(config, metadata_path, run_dir, force=False)->dict`; CLI `rnaforge semantic`.

- [ ] **Step 1: Failing test:**
```python
def test_run_semantic_requires_go_source(tmp_path): ...   # m09/m11 yok -> ValueError
def test_run_semantic_requires_obo(tmp_path): ...          # obo None -> hata
def test_run_semantic_reduces_ora(tmp_path): ...           # enrichment_up.tsv var -> reduced_ora_up.tsv + stats
def test_run_semantic_no_gate(tmp_path): ...
def test_run_semantic_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m12_semantic.py`: dizin `semantic/`, RunState, resume. Ön koşul: m09 veya m11 done; değilse ValueError. obo yoksa hata. `parse_obo`+`build_gene2go`(IC için) → `compute_ic`. Kaynaklar: enrichment_up/down.tsv (varsa) namespace TSV'den; gsea_go.tsv (varsa) namespace `go_meta`'dan; her biri için anlamlı GO terimleri çıkar → `reduce_terms` → `reduced_*.tsv` (go_id,namespace,term,padj,n_collapsed,members). stats JSON. gate YOK. `cli.py`: `semantic` subparser + `_cmd_semantic` + dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m12): orchestrator + rnaforge semantic CLI`.

---

### Task 5: m08 raporda REVIGO bölümü

**Files:** Modify `rnaforge/report_html.py`; Test `tests/test_report_html.py`.
**Interfaces:** Consumes reduced_*.tsv. Produces "Anlamsal İndirgeme (REVIGO)" bölümü + yöntem/kaynak (semantic_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_semantic_present(tmp_path): ...   # reduced rows -> tablo + "N->M" özeti
def test_section_semantic_absent_note(): ...       # yok -> çalıştırılmadı notu
def test_methods_refs_semantic_when_ran(): ...      # Supek/REVIGO kaynağı
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_reduced_tsv`. `load_report_inputs`: reduced_ora_up/down/gsea_go (yoksa None). `section_semantic`: kaynak başına temsilci tablo (term,namespace,padj,n_collapsed) + "N terim→M temsilci". LABELS + SECTION_INTRO + `_SEMANTIC_REFERENCES` (Lin doi:10.5555/... yoksa Supek 2011 doi:10.1371/journal.pone.0021800). `section_methods(...,semantic_ran)` + `section_references(...,semantic_ran)`. `render_report`: semantic_ran; section_semantic GSEA'dan sonra; N_SECTIONS 11→12.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m12): m08 raporda REVIGO bolumu + yontem/kaynak`.

---

### Task 6: GSE300731 canlı smoke

- [ ] **Step 1:** `rnaforge semantic --run-id GSE300731` (obo config'te var; m09+m11 çalışmış).
- [ ] **Step 2:** `reduced_ora_up.tsv` incele: 58 terim → kaç temsilci? "polysaccharide biosynthetic/metabolic" ailesi tek temsilcide mi?
- [ ] **Step 3:** `rnaforge report --force` → REVIGO bölümü + "N→M" özeti; verdict SUSPECT değişmez.
- [ ] **Step 4:** Doğrulama notu.

---

## Self-Review

**Spec coverage:** §2 IC/Lin→Task 2; §3 reduce→Task 3; §4 orkestrasyon/CLI→Task 4; §5 config→Task 1; §6 rapor→Task 5; §7 doğrulama→testler+Task 6. Kapsanıyor.
**Placeholder scan:** somut imza+algoritma.
**Type consistency:** `compute_ic`/`lin_similarity`→`reduce_terms`→m12; `reduce_terms` çıktı sütunları reduced TSV ile aynı; `parse_reduced_tsv`↔yazım.
