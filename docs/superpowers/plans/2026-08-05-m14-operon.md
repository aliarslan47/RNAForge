# m14 Operon Analizi Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Intergenik-mesafe sezgiseliyle operon tahmini + operon-düzeyi DE koordinasyonu (m14).

**Architecture:** GFF gene koordinatları → aynı-strand + gap≤max_gap operonları → deseq2 DE agregasyonu (koordineli operonlar) → operon/operons.tsv → m08 Operon bölümü. Saf Python; gate yok; figür yok. Zincir m06 tabanlı.

**Tech Stack:** Python 3.11 stdlib, pytest.

## Global Constraints

- Saf Python stdlib. Operon = aynı strand + gap≤max_gap ardışık genler; strand/contig/gap kırılımı.
- Gate YOK; verdict m06/m07'den taşınır. Operonlar TAHMİN (rapora dürüst not). DE eşikleri config.de.
- Config `operon.max_gap` (50 bp). GFF gerekli; m06 ön koşul.
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`

---

### Task 1: Config OperonConfig

**Files:** Modify `rnaforge/config.py`; Test `tests/test_config.py`.
**Interfaces:** Produces `Config.operon: OperonConfig(max_gap=50)`; `"operon"` in KNOWN_TOP_LEVEL_KEYS.

- [ ] **Step 1: Failing test:**
```python
def test_operon_config_default(tmp_path):
    assert load_config(_write(tmp_path, PROK_BODY)).operon.max_gap == 50
def test_operon_config_parsed(tmp_path):
    assert load_config(_write(tmp_path, PROK_BODY + "\noperon:\n  max_gap: 100\n")).operon.max_gap == 100
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `OperonConfig(max_gap:int=50)` frozen; `Config.operon` (default_factory); `"operon"` KNOWN_TOP_LEVEL_KEYS'e; parse `_as_int(operon_raw.get("max_gap",50),...)`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m14): config OperonConfig`.

---

### Task 2: predict_operons

**Files:** Create `rnaforge/operon.py`; Test `tests/test_operon.py`.
**Interfaces:** Produces `predict_operons(gff, max_gap=50) -> list[dict]` (operon_id, contig, strand, locus_tags, symbols, size).

- [ ] **Step 1: Failing test:**
```python
GFF = (  # A,B bitişik + genişletilebilir; C uzak; D ters strand
  "c1\tx\tgene\t100\t200\t.\t+\t.\tlocus_tag=A;gene=a\n"
  "c1\tx\tgene\t210\t300\t.\t+\t.\tlocus_tag=B;gene=b\n"   # gap 9 -> A ile aynı operon
  "c1\tx\tgene\t500\t600\t.\t+\t.\tlocus_tag=C;gene=c\n"   # gap 199 -> yeni operon
  "c1\tx\tgene\t610\t700\t.\t-\t.\tlocus_tag=D;gene=d\n")  # ters strand -> yeni operon
def test_predict_operons_groups_and_breaks(tmp_path):
    ops = predict_operons(gff, max_gap=50)
    assert [o["locus_tags"] for o in ops] == [["A","B"],["C"],["D"]]
    assert ops[0]["strand"]=="+" and ops[0]["size"]==2
def test_predict_operons_contig_break(tmp_path): ...  # farklı contig ayrı operon
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** GFF `\tgene\t` satırları → {contig,start,end,strand,locus_tag,symbol}; (contig,start) sırala; ardışık: `same contig & same strand & (start - prev_end - 1) <= max_gap` → aynı operona ekle, değilse yeni operon başlat. operon_id `operon_<n>`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m14): operon prediction (intergenic distance)`.

---

### Task 3: aggregate_operon_de

**Files:** Modify `rnaforge/operon.py`; Test `tests/test_operon.py`.
**Interfaces:** Produces `aggregate_operon_de(operons, deseq_tsv, fdr, lfc) -> list[dict]` (+ size,n_tested,n_deg,n_up,n_down,mean_log2fc,coordinated).

- [ ] **Step 1: Failing test:**
```python
def test_aggregate_coordinated_operon(tmp_path):
    # A,B ikisi up -> coordinated True, n_up=2
def test_aggregate_mixed_not_coordinated(tmp_path):
    # A up, B down -> coordinated False
def test_aggregate_single_gene_not_coordinated(): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** deseq oku (gene→log2FC/padj); operon başına: tested genler, n_deg (padj<fdr & |lfc|≥thr), n_up/n_down, mean_log2fc; `coordinated = size>=2 and n_deg>=2 and (n_up==n_deg or n_down==n_deg)`. Sırala (coordinated önce, n_deg desc).
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m14): operon-level DE aggregation`.

---

### Task 4: m14 orkestratör + CLI

**Files:** Create `rnaforge/modules/m14_operon.py`; Modify `rnaforge/cli.py`; Test `tests/test_m14_operon.py`.
**Interfaces:** Produces `run_operon(config, metadata_path, run_dir, force=False)->dict`; CLI `rnaforge operon`.

- [ ] **Step 1: Failing test:**
```python
def test_run_operon_requires_m06(tmp_path): ...
def test_run_operon_writes_table(tmp_path): ...   # operons.tsv + stats + gate yok
def test_run_operon_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m14_operon.py`: dizin `operon/`, RunState, resume, m06 ön koşul. `predict_operons(gff, max_gap)` → `aggregate_operon_de(deseq, fdr, lfc)` → `operons.tsv` (genes=symbols `;`). stats (n_operons, n_multi, n_coordinated, max_gap). gate YOK. `cli.py`: `operon` subparser + `_cmd_operon` + dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m14): orchestrator + rnaforge operon CLI`.

---

### Task 5: m08 raporda Operon bölümü

**Files:** Modify `rnaforge/report_html.py`; Test `tests/test_report_html.py`.
**Interfaces:** Consumes operons.tsv + operon_stats. Produces "Operon Analizi" bölümü + yöntem/kaynak (operon_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_operon_present(tmp_path): ...   # koordineli operon tablosu + "tahmin" notu
def test_section_operon_absent_note(): ...
def test_methods_refs_operon_when_ran(): ...      # Moreno-Hagelsieb kaynağı
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_operon_tsv`. `load_report_inputs`: operons (yoksa None) + operon_stats. `section_operon`: koordineli operonlar tablosu (genes, size, n_deg, yön, mean_log2fc) + özet + "tahmin" dürüst notu. LABELS + intro + `_OPERON_REFERENCES` (Moreno-Hagelsieb & Collado-Vides 2002 doi:10.1093/nar/30.6.1362). `section_methods(...,operon_ran)` + `section_references(...,operon_ran)`. `render_report`: operon_ran; section_operon AMR'dan sonra; N_SECTIONS 13→14.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m14): m08 raporda Operon bolumu + yontem/kaynak`.

---

### Task 6: GSE300731 canlı smoke

- [ ] **Step 1:** GSE300731 config'ine `operon:` (varsayılan yeterli). `rnaforge operon --run-id GSE300731`.
- [ ] **Step 2:** `operons.tsv` incele: thrABC operonu var mı; ent*/acr/emr operonları koordineli DE mi.
- [ ] **Step 3:** `rnaforge report --force` → Operon bölümü + tahmin notu; verdict SUSPECT değişmez.
- [ ] **Step 4:** Doğrulama notu.

---

## Self-Review

**Spec coverage:** §2 tahmin→Task 2; §3 agregasyon→Task 3; §4 orkestrasyon/CLI→Task 4; §5 rapor→Task 5; §6 config→Task 1; §7 doğrulama→testler+Task 6. Kapsanıyor.
**Placeholder scan:** somut.
**Type consistency:** `predict_operons`→`aggregate_operon_de`→m14; `parse_operon_tsv` sütunları m14 yazımıyla aynı.
