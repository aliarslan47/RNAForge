# m13 AMR + Virülans Overlay Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** abricate ile CARD (AMR) + VFDB (virülans) genlerini tespit edip DE durumuyla birleştiren m13 modülü.

**Architecture:** abricate (rnaforge-amr env) genome.fa tarar → parse + %id/%cov filtre → GFF koordinat kesişimiyle locus_tag → deseq2 DE overlay → amr/*.tsv → m08 AMR/VFDB bölümü. Gate yok. Zincir m06 tabanlı.

**Tech Stack:** Python 3.11 stdlib, abricate 1.4.0 (rnaforge-amr), pytest.

## Global Constraints

- Eşleme KOORDİNAT örtüşmesiyle (gen adı değil — abricate adı sembolle eşleşmeyebilir). En çok örtüşen gen.
- Eşleşmeyen hit loglanır (sessiz değil). Gate YOK; verdict m06/m07'den taşınır.
- DE eşikleri config.de.{fdr,log2fc}. abricate eşikleri config.amr.min_identity/coverage (80/80).
- abricate env config.amr.env (rnaforge-amr). Env/genome yoksa gürültülü hata.
- DB tarihi rapora dürüstçe yazılır (bundled DB güncel olmayabilir).
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`

---

### Task 1: Config AMRConfig + env yml

**Files:** Modify `rnaforge/config.py`, create `envs/rnaforge-amr.yml`; Test `tests/test_config.py`.
**Interfaces:** Produces `Config.amr: AMRConfig(amr_db="card", virulence_db="vfdb", env="rnaforge-amr", min_identity=80.0, min_coverage=80.0)`; `"amr"` in KNOWN_TOP_LEVEL_KEYS.

- [ ] **Step 1: Failing test:**
```python
def test_amr_config_defaults(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY))
    assert cfg.amr.amr_db == "card" and cfg.amr.virulence_db == "vfdb"
    assert cfg.amr.min_identity == 80.0 and cfg.amr.env == "rnaforge-amr"
def test_amr_config_parsed(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY + "\namr:\n  amr_db: ncbi\n  min_identity: 90\n"))
    assert cfg.amr.amr_db == "ncbi" and cfg.amr.min_identity == 90.0
def test_unknown_top_level_still_rejected(tmp_path): ...  # regresyon: bilinmeyen anahtar reddi
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `AMRConfig` frozen dataclass; `Config.amr` alanı (default_factory); `"amr"` KNOWN_TOP_LEVEL_KEYS'e; parse bloğu (`amr_raw=_section(raw,"amr")`, `_as_float`/`str`). `envs/rnaforge-amr.yml` = `conda env export -n rnaforge-amr --no-builds`.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m13): config AMRConfig + rnaforge-amr env`.

---

### Task 2: abricate parser

**Files:** Create `rnaforge/abricate.py`; Test `tests/test_abricate.py`.
**Interfaces:** Produces `parse_abricate(tsv, min_identity=80, min_coverage=80) -> list[dict]`.

- [ ] **Step 1: Failing test** (abricate 1.4 başlıklı küçük TSV, biri düşük %id → elenir):
```python
HEADER="#FILE\tSEQUENCE\tSTART\tEND\tSTRAND\tGENE\tCOVERAGE\tCOVERAGE_MAP\tGAPS\t%COVERAGE\t%IDENTITY\tDATABASE\tACCESSION\tPRODUCT\tRESISTANCE\n"
def test_parse_abricate_filters_and_types(tmp_path):
    hits = parse_abricate(tsv, 80, 80)
    assert hits[0]["gene"]=="acrB" and hits[0]["contig"]=="chr1" and hits[0]["start"]==100
    assert hits[0]["pct_id"]==99.5 and hits[0]["db"]=="card"
    assert all(h["pct_id"]>=80 for h in hits)   # düşük %id elendi
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_abricate`: `csv.DictReader` (başlıktaki `#` strip), her satır → dict; `%IDENTITY`/`%COVERAGE` float, START/END int; filtre; alan adları normalize (contig=SEQUENCE, gene=GENE, pct_id=%IDENTITY, pct_cov=%COVERAGE, db=DATABASE, product=PRODUCT, resistance=RESISTANCE).
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m13): abricate TSV parser`.

---

### Task 3: koordinat eşleme + DE overlay

**Files:** Modify `rnaforge/abricate.py`; Test `tests/test_abricate.py`.
**Interfaces:** Produces `gene_coords(gff)->list[dict]`; `map_hits_to_genes(hits, genes)->list[dict]` (locus_tag/symbol ekli, unmapped=None); `overlay_de(mapped, deseq_tsv, fdr, lfc)->list[dict]` (log2FC/padj/de_status ekli).

- [ ] **Step 1: Failing test:**
```python
def test_gene_coords_from_gff(tmp_path): ...   # gene feature -> contig/start/end/locus_tag/symbol
def test_map_hits_best_overlap(tmp_path):
    # hit iki gene örtüşürse en çok örtüşene; örtüşmeyen -> None
def test_overlay_de_status(tmp_path):
    # up/down/ns/untested doğru; NA padj -> untested
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `gene_coords`: GFF `\tgene\t` satırları, attrs'tan locus_tag/gene; contig=col0, start/end col3/4. `map_hits_to_genes`: her hit için aynı contig genlerde max örtüşme (`min(e,ge)-max(s,gs)`); >0 ise ata (locus_tag,symbol), değilse None. `overlay_de`: deseq oku (gene→log2FC/padj); de_status hesapla.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m13): coordinate mapping + DE overlay`.

---

### Task 4: m13 orkestratör + CLI

**Files:** Create `rnaforge/modules/m13_amr.py`; Modify `rnaforge/cli.py`; Test `tests/test_m13_amr.py`.
**Interfaces:** Produces `run_amr(config, metadata_path, run_dir, force=False)->dict`; CLI `rnaforge amr`.

- [ ] **Step 1: Failing test** (abricate monkeypatch):
```python
def test_run_amr_requires_m06(tmp_path): ...
def test_run_amr_requires_genome(tmp_path): ...   # genome_fasta yok -> hata
def test_run_amr_writes_tables(monkeypatch): ...   # amr_genes.tsv + virulence_genes.tsv + stats + gate yok
def test_run_amr_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m13_amr.py`: dizin `amr/`, RunState, resume, m06 ön koşul, genome yoksa hata. AMR: `run_abricate(genome, amr_db)`→parse→map(gene_coords)→overlay_de→`amr_genes.tsv`; aynısı VFDB→`virulence_genes.tsv`. `_write_amr_tsv`. stats JSON (n/de/unmapped/db). gate YOK. `cli.py`: `amr` subparser + `_cmd_amr` + dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m13): orchestrator + rnaforge amr CLI`.

---

### Task 5: m08 raporda AMR/VFDB bölümü

**Files:** Modify `rnaforge/report_html.py`; Test `tests/test_report_html.py`.
**Interfaces:** Consumes amr_genes.tsv/virulence_genes.tsv. Produces "Direnç ve Virülans" bölümü + yöntem/kaynak (amr_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_amr_present(tmp_path): ...   # tablo + DE durumu + DB tarih notu
def test_section_amr_absent_note(): ...
def test_methods_refs_amr_when_ran(): ...      # abricate/CARD/VFDB kaynağı
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_amr_tsv`. `load_report_inputs`: amr_genes/virulence_genes (yoksa None) + amr_stats. `section_amr`: AMR + virülans tablo (gene, sınıf/faktör, %id, de_status), DE durumu vurgu; DB tarih notu (amr_stats'tan). LABELS + intro + `_AMR_REFERENCES` (abricate/CARD Jia 2017 doi:10.1093/nar/gkw1004 / VFDB Liu 2019 doi:10.1093/nar/gky1080). `section_methods(...,amr_ran)` + `section_references(...,amr_ran)`. `render_report`: amr_ran; section_amr REVIGO'dan sonra; N_SECTIONS 12→13.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m13): m08 raporda AMR/VFDB bolumu + yontem/kaynak`.

---

### Task 6: GSE300731 canlı smoke

- [ ] **Step 1:** GSE300731 config'ine `amr:` bloğu (varsayılanlar yeterli — reference.genome_fasta zaten var). `rnaforge amr --run-id GSE300731`.
- [ ] **Step 2:** `amr_genes.tsv`/`virulence_genes.tsv` incele: intrinsik efflux (acrAB/mdt) var mı; enterololin altında DE mi. tolC silinmiş suş → tolC hit'i yok/eşleşmeyebilir (beklenen).
- [ ] **Step 3:** `rnaforge report --force` → AMR/VFDB bölümü + DB tarih notu; verdict SUSPECT değişmez.
- [ ] **Step 4:** Doğrulama notu.

---

## Self-Review

**Spec coverage:** §2 parser→Task 2; §3 eşleme/overlay→Task 3; §4 orkestrasyon/CLI→Task 4; §6 rapor→Task 5; §7 config/env→Task 1; §8 doğrulama→testler+Task 6. Kapsanıyor.
**Placeholder scan:** somut imza+algoritma.
**Type consistency:** `parse_abricate`→`map_hits_to_genes`→`overlay_de`→m13; `parse_amr_tsv` sütunları m13 yazımıyla aynı.
