# m11 GSEA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DESeq2 ranked listesinden fgsea ile GSEA (m11); m09/m10 gen-seti kurucularını yeniden kullanır.

**Architecture:** deseq2 stat → ranked.rnk; GO/KEGG gen setleri (build_gene2go/build_gene2pathway ters çevrilir) → GMT; fgsea (rnaforge-de) → gsea_{go,kegg}.tsv + NES dot-plot; m11 orkestratör (gate yok) → CLI gsea → m08 GSEA bölümü. Zincir m06→m07→m09/m10/m11→m08.

**Tech Stack:** Python 3.11 stdlib, R/fgsea+ggplot2 (rnaforge-de), pytest.

## Global Constraints

- Gen-seti kurucuları YENİDEN KULLANILIR: `go_annotation.build_gene2go`, `kegg_annotation.build_gene2pathway`.
- Ranking metriği: DESeq2 `stat` (Wald); NA atılır. GMT/rnk anahtarı = locus_tag (deseq gene sütunu).
- Motor fgsea (rnaforge-de env; `bioconductor-fgsea` kurulur + envs/rnaforge-de.yml). NES İŞARETLİ (up/down tek koşu).
- Gate YOK; verdict m06/m07'den taşınır. Eksik referans (obo/kegg) → o koleksiyon atlanır (sessiz DEĞİL, log+stats); hiç koleksiyon yoksa hata. `stat` yoksa gürültülü hata.
- fgsea eşikleri config: `enrichment.gsea_min_size`(15)/`gsea_max_size`(500).
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`

---

### Task 1: Config gsea eşikleri + env

**Files:**
- Modify: `rnaforge/config.py`, `envs/rnaforge-de.yml`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Enrichment.gsea_min_size:int=15`, `Enrichment.gsea_max_size:int=500`.

- [ ] **Step 1: Failing test:**
```python
def test_enrichment_gsea_sizes(tmp_path):
    body = PROK_BODY + "\nenrichment:\n  gsea_min_size: 10\n  gsea_max_size: 300\n"
    cfg = load_config(_write(tmp_path, body))
    assert cfg.enrichment.gsea_min_size == 10 and cfg.enrichment.gsea_max_size == 300

def test_enrichment_gsea_defaults(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY))
    assert cfg.enrichment.gsea_min_size == 15 and cfg.enrichment.gsea_max_size == 500
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `Enrichment`'e iki alan; parse `_as_int(enrichment_raw.get("gsea_min_size",15),...)` vb. `envs/rnaforge-de.yml`'e `- bioconductor-fgsea` ekle.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m11): config gsea min/max size + fgsea env`.

---

### Task 2: gsea.py — ranked list + GMT

**Files:**
- Create: `rnaforge/gsea.py`
- Test: `tests/test_gsea.py`

**Interfaces:**
- Produces: `write_rnk(deseq_tsv, out_path) -> int` (yazılan gen sayısı; NA stat atılır);
  `invert_to_gmt(gene2set:dict[str,set], meta:dict[str,tuple], out_path) -> int` (yazılan set sayısı).

- [ ] **Step 1: Failing test:**
```python
def test_write_rnk_drops_na_stat(tmp_path):
    (tmp_path/"de.tsv").write_text("gene\tbaseMean\tlog2FoldChange\tlfcSE\tstat\tpvalue\tpadj\n"
        "g1\t1\t2\t.3\t5.0\t1e-3\t1e-2\ng2\t1\tNA\tNA\tNA\tNA\tNA\n")
    n = write_rnk(tmp_path/"de.tsv", tmp_path/"r.rnk")
    assert n == 1
    assert (tmp_path/"r.rnk").read_text().splitlines()[0] == "g1\t5.0"

def test_invert_to_gmt(tmp_path):
    g2s = {"g1": {"GO:1"}, "g2": {"GO:1", "GO:2"}}
    meta = {"GO:1": ("BP","alpha"), "GO:2": ("BP","beta")}
    n = invert_to_gmt(g2s, meta, tmp_path/"x.gmt")
    lines = dict(l.split("\t",1) for l in (tmp_path/"x.gmt").read_text().splitlines())
    assert set(lines["GO:1"].split("\t")[1:]) == {"g1","g2"}   # desc + genes
    assert lines["GO:1"].split("\t")[0] == "alpha"
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `write_rnk`: deseq oku, gene+stat, NA/boş stat atla, `gene\tstat` yaz, sayı döndür. `invert_to_gmt`: set→genler; her satır `set_id\tname\tgene...`; namespace GMT'de gerekmez (meta name kullan). Boş setleri atla.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m11): ranked list (rnk) + GMT inverter`.

---

### Task 3: gsea.R + run_gsea_r (fgsea)

**Files:**
- Create: `rnaforge/scripts/gsea.R`
- Modify: `rnaforge/gsea.py`
- Test: `tests/test_gsea.py` (env-gated)

**Interfaces:**
- Produces: `run_gsea_r(rnk, gmt, gene_map, out_dir, collection, min_size, max_size, title, env="rnaforge-de") -> str`.

- [ ] **Step 1: Failing test** (skip if fgsea yok): küçük rnk (20 gen) + gmt (1 set 5 gen aynı yönde) → `run_gsea_r` → `gsea_test.tsv` var, NES sütunu dolu.
- [ ] **Step 2:** FAIL/skip.
- [ ] **Step 3:** `gsea.R`: args rnk gmt gene_map out_dir collection min max title. rnk oku → named vector (azalan). gmt → `fgsea::gmtPathways` benzeri (manuel: her satır set→genler). `fgsea(pathways, stats, minSize, maxSize)`. leadingEdge locus_tag→sembol (gene_map). TSV yaz (pathway_id/name/namespace? namespace GMT'de yok → meta ayrı; basit tut: pathway_id,name,size,ES,NES,pval,padj,leading_edge). NES dot-plot (top ±) ggplot → png/svg. `run_gsea_r`: conda run Rscript, hata gürültülü.
- [ ] **Step 4:** PASS/skip; elle env'de bir kez doğrula.
- [ ] **Step 5:** commit `feat(m11): gsea.R (fgsea) + NES dot-plot + runner`.

---

### Task 4: m11 orkestratör + CLI

**Files:**
- Create: `rnaforge/modules/m11_gsea.py`
- Modify: `rnaforge/cli.py`
- Test: `tests/test_m11_gsea.py`

**Interfaces:**
- Consumes: `write_rnk`, `invert_to_gmt`, `run_gsea_r`, `build_gene2go`, `parse_obo`, `build_gene2pathway`, `figures.write_gene_map`, `build_enrichment_manifest`, `RunState`.
- Produces: `run_gsea(config, metadata_path, run_dir, force=False) -> dict`; CLI `rnaforge gsea`.

- [ ] **Step 1: Failing test** (R monkeypatch):
```python
def test_run_gsea_requires_m06(tmp_path): ...
def test_run_gsea_requires_a_collection(tmp_path): ...   # obo yok + kegg_organism yok -> ValueError
def test_run_gsea_missing_stat_loud(tmp_path): ...        # deseq'te stat sütunu yok -> hata
def test_run_gsea_writes_outputs(monkeypatch): ...        # gsea_go.tsv + stats + gate yok
def test_run_gsea_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m11_gsea.py`: dizin `gsea/`, RunState, resume, m06 ön koşul. `write_rnk`; gene_map (write_gene_map). GO koleksiyonu: obo varsa `build_gene2go`→`invert_to_gmt(go.gmt)`; KEGG: kegg_organism+dosya varsa `build_gene2pathway`→`invert_to_gmt(kegg.gmt)`. En az bir koleksiyon yoksa hata (log'da hangisi neden atlandı). Her koleksiyon için `run_gsea_r`. manifest (gsea_go/gsea_kegg prefix — build_enrichment_manifest genelleştir veya basit kur). stats JSON. gate YOK. `cli.py`: `gsea` subparser + `_cmd_gsea` + dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m11): orchestrator + rnaforge gsea CLI`.

---

### Task 5: m08 raporda GSEA bölümü

**Files:**
- Modify: `rnaforge/report_html.py`
- Test: `tests/test_report_html.py`

**Interfaces:**
- Consumes: `gsea_go.tsv`, `gsea_kegg.tsv`, gsea manifest.
- Produces: "Gen Seti Zenginleştirme (GSEA)" bölümü + yöntem/kaynak (gsea_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_gsea_present(tmp_path):
    inputs = {"gsea_go": _gsea_rows, "gsea_kegg": None, "gsea_manifest": None, "gsea_dir": tmp_path}
    html = section_gsea(inputs, LABELS["tr"], "tr")
    assert "GSEA" in html and "NES" in html and "<pathway>" in html
def test_section_gsea_absent_note():
    assert "çalıştırılmadı" in section_gsea({"gsea_go":None,"gsea_kegg":None}, LABELS["tr"], "tr")
def test_methods_refs_gsea_when_ran():
    assert "GSEA" in section_methods(cfg, LABELS["tr"], gsea_ran=True)
    assert "Subramanian" in section_references(LABELS["en"], gsea_ran=True) or "1073/pnas" in ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_gsea_tsv` (pathway_id,name,size,ES,NES,pval,padj,leading_edge tipli). `load_report_inputs`: gsea_go/gsea_kegg/manifest/dir (yoksa None). `section_gsea`: koleksiyon başına top +NES / top −NES tablo (term, NES, padj, size, öncü genler) + gömülü figür; tolerant not. LABELS gsea etiketleri. `section_methods(...,gsea_ran=False)` + `section_references(...,gsea_ran=False)`: GSEA cümlesi + `_GSEA_REFERENCES` (Subramanian 2005 doi:10.1073/pnas.0506580102 + Korotkevich fgsea doi:10.1101/060012). `render_report`: `gsea_ran`; section_gsea'yı enrichment'tan sonra ekle; wire.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m11): m08 raporda GSEA bölümü + yöntem/kaynak`.

---

### Task 6: GSE300731 canlı smoke

- [ ] **Step 1:** fgsea kurulu teyit (`requireNamespace fgsea`). GSE300731 config'i (obo+kegg zaten var) → `rnaforge gsea --run-id GSEA300731`... `--run-id GSE300731`.
- [ ] **Step 2:** `gsea_go.tsv`/`gsea_kegg.tsv` + figür incele.
- [ ] **Step 3:** Biyolojik akıl: pozitif NES (artan) = kolanik asit/peptidoglikan/zarf-stres; negatif NES (azalan) = oksidatif fosforilasyon/respirasyon. ORA ile uyum.
- [ ] **Step 4:** `rnaforge report --force` → GSEA bölümü gömülü; verdict SUSPECT değişmez.
- [ ] **Step 5:** Doğrulama notu (commit gerekmez; kod zaten commit'li).

---

## Self-Review

**Spec coverage:** §2 girdi→Task 2; §3 motor→Task 3; §4 orkestrasyon/CLI→Task 4; §5 config→Task 1; §6 rapor→Task 5; §8 doğrulama→testler + Task 6. Kapsanıyor.
**Placeholder scan:** somut; fixture'lar kısaltılmış ama imza+algoritma net.
**Type consistency:** `write_rnk`/`invert_to_gmt`→Task 4; `run_gsea_r` args Task 3↔4; `parse_gsea_tsv` sütunları gsea.R çıktısıyla aynı.
