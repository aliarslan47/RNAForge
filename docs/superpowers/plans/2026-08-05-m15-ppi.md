# m15 PPI + Community Detection Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** STRING PPI alt-ağı + Louvain community (networkx) ile DEG fonksiyonel modülleri (m15).

**Architecture:** STRING links/info → sembol-join locus_tag → DEG-DEG alt-ağı (networkx) → louvain_communities → ppi/communities.tsv → m08 PPI bölümü. Gate yok; figür yok. Zincir m06 tabanlı.

**Tech Stack:** Python 3.11 + networkx (rnaforge-core), pytest.

## Global Constraints

- STRING links BOŞLUKLA ayrılmış (`p1 p2 score`); info TAB. Sembol-join (m09 `_symbol_to_locus`, belirsiz atılır).
- Louvain networkx (`louvain_communities`, seed=42 deterministik). Elle yazma yok.
- Gate YOK; verdict m06/m07'den taşınır. STRING etkileşim = kanıt-skorlu (rapora dürüst not).
- Eşik `ppi.min_score`(700). DEG eşikleri config.de. taxid/dosya yoksa gürültülü hata.
- Test: `conda run -n rnaforge-core --cwd /home/ali/rnaforge-pipeline python -m pytest -q`

---

### Task 1: Config PPIConfig + env yml

**Files:** Modify `rnaforge/config.py`, `envs/rnaforge-core.yml` (networkx); Test `tests/test_config.py`.
**Interfaces:** Produces `Config.ppi: PPIConfig(taxid=None, string_dir=None, min_score=700, min_community_size=3)`; `"ppi"` in KNOWN_TOP_LEVEL_KEYS.

- [ ] **Step 1: Failing test:**
```python
def test_ppi_config_defaults(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY))
    assert cfg.ppi.taxid is None and cfg.ppi.min_score == 700 and cfg.ppi.min_community_size == 3
def test_ppi_config_parsed(tmp_path):
    cfg = load_config(_write(tmp_path, PROK_BODY + "\nppi:\n  taxid: '511145'\n  min_score: 400\n"))
    assert cfg.ppi.taxid == "511145" and cfg.ppi.min_score == 400
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `PPIConfig` frozen; `Config.ppi` (default_factory); `"ppi"` KNOWN_TOP_LEVEL_KEYS'e; parse (`ppi_raw`, `str`/`_as_int`/Path). `envs/rnaforge-core.yml` = `conda env export` (networkx dahil).
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m15): config PPIConfig + networkx env`.

---

### Task 2: STRING parser + sembol join

**Files:** Create `rnaforge/ppi.py`; Test `tests/test_ppi.py`.
**Interfaces:** Produces `parse_string_info(info_gz)->dict[str,str]`; `parse_string_links(links_gz, min_score)->list[tuple]`; `string_to_locus(info, gene_symbol)->dict[str,str]`.

- [ ] **Step 1: Failing test** (gzip küçük fixture):
```python
def test_parse_string_info(tmp_path): ...   # #header + 511145.b0001\tthrL -> {"511145.b0001":"thrL"}
def test_parse_string_links_filter(tmp_path):
    # boşlukla; score>=eşik -> tut, altı at
def test_string_to_locus_symbol_join(tmp_path):
    # string_id -> symbol -> locus_tag; belirsiz sembol atılır
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_string_info`: gzip.open, tab, `#` başlığı atla, id->preferred_name. `parse_string_links`: gzip, header atla, `line.split()` (boşluk), score int, eşik filtre. `string_to_locus`: info sembolü + `go_annotation._symbol_to_locus(gene_symbol)` ters → string_id→locus_tag.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m15): STRING parser + symbol join`.

---

### Task 3: alt-ağ + community

**Files:** Modify `rnaforge/ppi.py`; Test `tests/test_ppi.py`.
**Interfaces:** Produces `build_deg_network(deg_ids, edges, string2lt)->nx.Graph`; `detect_communities(G, seed=42)->list[list]`; `summarize_communities(comms, gene_symbol, de, min_size)->list[dict]`.

- [ ] **Step 1: Failing test:**
```python
def test_build_network_only_deg_deg(tmp_path):
    # kenarın iki ucu da DEG ise düğüm/kenar; biri değilse atla; weight=score/1000
def test_detect_communities_deterministic():
    # iki ayrık üçgen -> 2 community; seed sabit -> tekrarlanabilir
def test_summarize_direction_and_filter():
    # modül üye sembol, n_up/n_down, dominant; <min_size elenir
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `build_deg_network`: nx.Graph; her edge (a,b,score) için a,b string2lt'de ve locus_tag DEG setinde ise `G.add_edge(lt_a, lt_b, weight=score/1000)`. `detect_communities`: `nx.community.louvain_communities(G, weight="weight", seed=seed)`. `summarize_communities`: her community için sembol/yön/boyut; `< min_size` atla; boyuta göre sırala.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m15): DEG subnetwork + Louvain communities`.

---

### Task 4: m15 orkestratör + CLI

**Files:** Create `rnaforge/modules/m15_ppi.py`; Modify `rnaforge/cli.py`; Test `tests/test_m15_ppi.py`.
**Interfaces:** Produces `run_ppi(config, metadata_path, run_dir, force=False)->dict`; CLI `rnaforge ppi`.

- [ ] **Step 1: Failing test:**
```python
def test_run_ppi_requires_m06(tmp_path): ...
def test_run_ppi_requires_taxid(tmp_path): ...      # taxid None -> hata
def test_run_ppi_missing_string_loud(tmp_path): ...  # dosya yok -> FileNotFoundError
def test_run_ppi_writes_communities(tmp_path): ...   # communities.tsv + stats + gate yok
def test_run_ppi_resume(tmp_path): ...
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `m15_ppi.py`: dizin `ppi/`, RunState, resume, m06 ön koşul, taxid/dosya yoksa hata. `parse_gff_go`→gene_symbol; `parse_string_info`→`string_to_locus`; `deg_sets` (enrichment.py yeniden) veya kendi DEG çıkarımı; `parse_string_links(min_score)`→`build_deg_network`→`detect_communities`→`summarize_communities`→`communities.tsv`. stats. gate YOK. `cli.py`: `ppi` subparser + `_cmd_ppi` + dispatch.
- [ ] **Step 4:** PASS.
- [ ] **Step 5:** commit `feat(m15): orchestrator + rnaforge ppi CLI`.

---

### Task 5: m08 raporda PPI bölümü

**Files:** Modify `rnaforge/report_html.py`; Test `tests/test_report_html.py`.
**Interfaces:** Consumes communities.tsv + ppi_stats. Produces "Protein Etkileşim Modülleri" bölümü + yöntem/kaynak (ppi_ran).

- [ ] **Step 1: Failing test:**
```python
def test_section_ppi_present(tmp_path): ...   # modül tablosu + özet + STRING notu
def test_section_ppi_absent_note(): ...
def test_methods_refs_ppi_when_ran(): ...      # STRING/Louvain kaynağı
```
- [ ] **Step 2:** FAIL.
- [ ] **Step 3:** `parse_community_tsv`. `load_report_inputs`: communities (yoksa None) + ppi_stats. `section_ppi`: en büyük modüller tablosu (genes, size, yön) + özet + STRING kanıt-skoru dürüst notu. LABELS + intro + `_PPI_REFERENCES` (STRING Szklarczyk 2023 doi:10.1093/nar/gkac1000 + Louvain Blondel 2008 doi:10.1088/1742-5468/2008/10/P10008). `section_methods(...,ppi_ran)` + `section_references(...,ppi_ran)`. `render_report`: ppi_ran; section_ppi Operon'dan sonra; N_SECTIONS 14→15.
- [ ] **Step 4:** PASS. Tüm suit yeşil.
- [ ] **Step 5:** commit `feat(m15): m08 raporda PPI bolumu + yontem/kaynak`.

---

### Task 6: GSE300731 canlı smoke

- [ ] **Step 1:** GSE300731 config'ine `ppi:\n  taxid: '511145'`. `rnaforge ppi --run-id GSE300731`.
- [ ] **Step 2:** `communities.tsv` incele: wca kapsül / ribozom / efflux / siderofor modülleri var mı (operon/GO ile uyumlu).
- [ ] **Step 3:** `rnaforge report --force` → PPI bölümü + STRING notu; verdict SUSPECT değişmez.
- [ ] **Step 4:** Doğrulama notu.

---

## Self-Review

**Spec coverage:** §2 parser→Task 2; §3 ağ/community→Task 3; §4 orkestrasyon/CLI→Task 4; §5 rapor→Task 5; §6 config/env→Task 1; §7 doğrulama→testler+Task 6. Kapsanıyor.
**Placeholder scan:** somut.
**Type consistency:** `parse_string_info`/`string_to_locus`→`build_deg_network`→m15; `parse_community_tsv` sütunları m15 yazımıyla aynı.
